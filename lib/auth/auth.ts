import type { AwsRoleConnection } from "@/types/aws";
export function requireRoleConnection(input: unknown): AwsRoleConnection { if (!input || typeof input !== "object") throw new Error("AWS connection details are required."); const value = input as Record<string, unknown>; if (typeof value.roleArn !== "string" || !/^arn:aws:iam::\d{12}:role\/.+/.test(value.roleArn)) throw new Error("Enter a valid IAM role ARN."); if (typeof value.region !== "string" || !/^[a-z]{2}(-gov)?-[a-z]+-\d$/.test(value.region)) throw new Error("Enter a valid AWS region."); return { roleArn: value.roleArn, region: value.region, externalId: typeof value.externalId === "string" ? value.externalId : undefined }; }
